import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#459a68",
      light: "#bfd054",
    },
    secondary: {
      main: "#60605d",
    },
    text: {
      primary: "#41413f",
    },
  },
});

export default theme;
